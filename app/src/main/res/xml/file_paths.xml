<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- The downloaded update APK lives in cacheDir; shared read-only with the
         system package installer via FileProvider. -->
    <cache-path name="updates" path="." />
</paths>
